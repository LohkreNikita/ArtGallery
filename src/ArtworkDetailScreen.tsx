// ArtworkDetailScreen.tsx
import React from 'react';
import { View, Text, Image, ScrollView } from 'react-native';

const ArtworkDetailScreen = ({  }) => {
//   const { artwork } = route.params;

  return (
    <ScrollView contentContainerStyle={{ padding: 20 }}>
      {/* <Image source={{ uri: artwork.webImage.url }} style={{ width: '100%', height: 300, borderRadius: 8 }} />
      <Text style={{ fontSize: 18, fontWeight: 'bold', marginTop: 20, marginBottom: 10 }}>{artwork.title}</Text>
      <Text style={{ marginBottom: 10 }}>{artwork.principalOrFirstMaker}</Text>
      <Text style={{ marginBottom: 20 }}>{artwork.longTitle}</Text> */}

      <Text>Hello</Text>
    </ScrollView>
  );
};

export default ArtworkDetailScreen;
